services:
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: phantom_adguard
    restart: unless-stopped
    ports:
      - "3053:3000"
      - "5353:53/tcp"
      - "5353:53/udp"
    volumes:
      - ./work:/opt/adguardhome/work
      - ./conf:/opt/adguardhome/conf
