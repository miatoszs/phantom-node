version: '3.8'

services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: phantom_uptime_kuma
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    volumes:
      - ./data:/app/data
